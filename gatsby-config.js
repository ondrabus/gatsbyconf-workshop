/** @type {import('gatsby').GatsbyConfig} */
module.exports = {
  siteMetadata: {
      title: ``,
      siteUrl: `https://www.yourdomain.tld`,
  },
  plugins: [
    // TODO: add Kontent source plugin
    `gatsby-plugin-react-helmet`
  ]
}
