import React from "react"
import { graphql } from "gatsby"

export default function LandingPageTemplate({data}: any){
    
    // get data using graphQL (via pageQuery) and resolve into components

}

export const pageQuery = graphql`
`
